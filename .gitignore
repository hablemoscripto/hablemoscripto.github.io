# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Marketing playbook — kept local, contains strategy and outreach templates
marketing/

# Scratch / screenshots — local only
temp/

# Supabase CLI local state (contains pooler URL, project refs)
supabase/.temp/

# Database backups / dumps — contain user PII, password hashes, auth tokens. Never commit.
backup-*.sql
*.dump
db-dump*.sql

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
