// Kept in a leaf module with zero imports: GamificationContext (entry chunk)
// needs REVIEW_XP, and importing it from dailyReviewService would drag the
// whole courseData bundle (~95 kB gz) into the landing-page chunk.
export const REVIEW_XP = 15;
export const MAX_REVIEWS_PER_DAY = 3;
